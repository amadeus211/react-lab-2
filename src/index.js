import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // Припустимо, що ваш головний компонент називається App

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
